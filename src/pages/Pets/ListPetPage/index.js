import React from 'react';
import PetList from '../../../containers/pet-list';

const PetListPage = () => {
    return (
        <div>
            <PetList/>
        </div>
    );
};

export default PetListPage;